import {Container, Col, Row} from "react-bootstrap"
import styles from "./LoginLink.module.css"
export const LoginLink = () => {
    return (
        <Container className={styles.containerColor}>
            Log in
        </Container>
    )
}